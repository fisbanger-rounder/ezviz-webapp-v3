name: CI/CD Pipeline

on:
  push:
    branches: [ "master", "main" ]
  pull_request:
    branches: [ "master", "main" ]
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Lint code
        run: npm run lint

      - name: Build application
        run: npm run build

  docker-build:
    name: Docker Build Validation
    runs-on: ubuntu-latest
    needs: build-and-test

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker Image (Verify Dockerfile)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: false # Dry-run build to verify the Dockerfile works
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # Uncomment and configure the deployment job below when ready to deploy
  # deploy:
  #   name: Deploy Application
  #   runs-on: ubuntu-latest
  #   needs: [build-and-test, docker-build]
  #   if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
  #
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #
  #     # Example 1: Deploy to GitHub Pages (Static website)
  #     # - name: Deploy to GitHub Pages
  #     #   uses: JamesIves/github-pages-deploy-action@v4
  #     #   with:
  #     #     folder: dist # The folder where build outputs are generated
  #
  #     # Example 2: Push Docker Image to Github Container Registry (GHCR)
  #     # - name: Log in to GHCR
  #     #   uses: docker/login-action@v3
  #     #   with:
  #     #     registry: ghcr.io
  #     #     username: ${{ github.actor }}
  #     #     password: ${{ secrets.GITHUB_TOKEN }}
  #     #
  #     # - name: Build and Push Docker Image
  #     #   uses: docker/build-push-action@v6
  #     #   with:
  #     #     context: .
  #     #     file: ./Dockerfile
  #     #     push: true
  #     #     tags: ghcr.io/${{ github.repository }}/ezviz-web-app:latest
